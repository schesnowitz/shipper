<%= @shipment.name %>
<%= @shipment.description %>
<%= @shipment.budget %>
<%= @shipment.location %>
<%= @shipment.open_for_bids %>
<%= @shipment.winning_bid %>
about <%= time_ago_in_words(@shipment.created_at) %> ago

<%= link_to "Edit Shipment", edit_shipment_path(@shipment) %>
<%= link_to "Delete Shipment", @shipment, method: :delete, data: {confirm: "Are you sure you want to delete this shipment?"} %>

<script src="https://maps.googleapis.com/maps/api/js?v=3.exp&AMP;sensor=false">
</script>

<script>
  //google map
  function initialize() {
    
down vote
var request = { 
            origin: "33.661565,73.041330",
            destination: "33.662502,73.044061",
            travelMode: google.maps.TravelMode.DRIVING
};
    var mapOptions = {
      zoom: 20,
      center: myLatlng,
      mapTypeId: google.maps.MapTypeId.ROADMAP
    }
        var map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);
    var marker = new google.maps.Marker({
      position: myLatlng,
      map: map,
      title: 'Contact'
    }
                                       );
  }
  google.maps.event.addDomListener(window, 'load', initialize);
</script>

                <!--container end-->
                <!--google map start-->
                <div class="contact-map">
                  <div id="map-canvas" style="width: 100%; height: 400px">
                  </div>
                </div>
                <!--google map end-->
                
                
